# TourPlanIt 최종 배포·제출 체크리스트

> 원칙: 이 문서의 항목이 끝나기 전에는 Netlify 운영 배포를 하지 않는다. 개발 확인은 로컬에서만 진행한다.

## 1. 제품 QA

- [ ] 관광지 탐색 → 기획안 생성 → 일정 수정 → 견적 → 홍보 문구 → PDF/공유를 한 건으로 점검
- [ ] 데스크톱, 태블릿, 폴드 와이드, 390px 모바일에서 버튼 겹침·가로 스크롤·긴 제목 잘림 점검
- [ ] AI·관광 데이터 실패 시 사용자가 이해할 수 있는 오류 문구와 재시도 경로 점검
- [ ] 공유 URL을 새 브라우저에서 열어 본문과 산출물이 정상인지 점검
- [ ] 실제 확정 가격·운영시간·예약 가능 여부는 자동 문구가 아닌 담당자 검토 항목으로 남는지 점검
- [x] 결과 화면에 숙박·식사·이동·관광 데이터 사용 여부를 확인하는 제출 전 점검을 표시
- [x] 예상 판매가를 확정 견적처럼 보이지 않도록 `참고 예산 범위`로 표기
- [x] 기획서 JSON 백업과 개인정보 없는 공유 전 확인 절차 제공

## 2. Netlify 최종 배포 전 보안

- [ ] `ANTHROPIC_API_KEY`를 Netlify 서버 환경변수에만 등록
- [ ] `KTO_API_KEY`를 Netlify 서버 환경변수에만 등록
- [ ] `VITE_` 접두사의 키, JSONBin 키, 브라우저 직접 AI 호출이 소스에 없는지 검색
- [ ] 브라우저 개발자 도구/빌드 결과에 API 키가 노출되지 않는지 점검
- [ ] Function `/.netlify/functions/ai-draft`, `/.netlify/functions/kto-proxy`의 정상·오류 응답 점검

## 3. 공모전 제출 문서

- [x] 제품 소개서: 문제·대상 사용자·핵심 효과
- [x] 기능 명세서: 입력 → 데이터 탐색 → AI 초안 → 담당자 검토 → 산출물
- [x] 관광 데이터 활용 설명서: 데이터 항목·활용 화면·출처·한계
- [x] 3분 데모 시나리오 및 전후 비교 화면
- [ ] 데스크톱·폴드/모바일 화면 캡처
- [x] 개인정보 비저장·AI 초안 검토 원칙·외부 데이터 최신성 고지

### 관광데이터 활용 증빙

- [ ] 생성 완료 화면의 `관광 데이터 근거`에서 `실시간 연동` 배지, API명, 조회 지역 코드, 조회 시각, 관광지 목록이 함께 보이도록 캡처한다.
- [ ] 기능설명서에 `한국관광공사 관광정보 OpenAPI (KorService1 / areaBasedList1)` → 지역 관광지 후보 조회 → AI 기획 초안 입력 흐름을 1장으로 설명한다.
- [ ] API 호출 실패 시 `기본 데이터`와 `재확인 필요`가 표시되며, 이를 실시간 관광공사 데이터로 주장하지 않는지 확인한다.
- [ ] 공개 화면·영상·PDF에는 API 키, 요청 URL의 serviceKey 등 비밀 정보가 보이지 않는지 확인한다.

완료 산출물: `documents/TourPlanIt_제품소개서_데모가이드_2026-08.pdf` (4쪽). 제출용 PDF는 2026-08-11에 페이지 렌더링으로 한글·표·여백을 시각 검수했다. DOCX는 수정 가능한 원본으로 함께 보관한다.

## 3-1. 현재 MVP의 범위

- 관광 데이터 후보와 AI 기획서는 **초안 생성·검토 도구**다. 관광지 운영시간, 예약 가능 여부, 실제 항공·숙소·식당 단가를 확정하지 않는다.
- 견적 탭과 화면의 가격은 지역 평균 단가를 기반으로 한 참고 예산이다. 고객에게 제시할 확정 견적은 협력사 조건과 인원으로 별도 검토한다.
- 기획서는 현재 브라우저 로컬 저장 방식이다. 다른 기기에서도 보관하려면 `백업 파일`을 내려받아야 한다.
- 공유 링크에는 기획서 본문이 포함된다. 개인정보·계약조건·원가가 포함된 문서는 공유하지 않는다.

## 4. 단일 배포 절차

1. 로컬 `npm run build`를 통과시킨다.
2. 최종 커밋과 제출 문서를 확정한다.
3. Netlify 환경변수를 등록한다.
4. 한 번 배포한다.
5. 운영 URL에서 관광 데이터·AI 초안·공유·PDF와 모바일 화면을 최종 확인한다.

## 5. 2026-08-11 내부 마감 검증

- [x] `npm run build` 통과
- [x] `git diff --check` 통과
- [x] 소스에서 브라우저 공개형 비밀키(`VITE_*KEY`, `VITE_*TOKEN`), JSONBin, 직접 Anthropic 키 패턴이 없는지 정적 검색
- [x] 제출용 PDF 4쪽 렌더링 검수
- [ ] Netlify 서버 환경변수 등록 및 운영 URL 최종 검증 — 단일 배포 시점에만 수행
