[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "20"

# SPA 라우팅: /.netlify/functions/* 는 Netlify가 자동 처리하므로 별도 규칙 불필요
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
