# TourPlanIt 리디자인 기준

## 목적

TourPlanIt을 단순한 AI 생성 화면이 아니라, 여행사가 상품 기획 결과를 검토하고 바로 활용하는 **상품 기획 워크스페이스**로 정리한다. 보기 좋은 카드의 양보다 “지금 무엇을 입력하고, 무엇을 검토하고, 어떤 산출물을 내보낼지”가 한눈에 보이는 구성을 우선한다.

## 적용한 공통 기반

- 중립적인 슬레이트 계열 텍스트와 단일 브랜드 블루를 기본 위계로 사용한다.
- 노랑·초록·빨강·보라는 상태/강조에만 사용한다. 모든 카드가 각기 다른 색을 경쟁하지 않게 한다.
- 시스템 폰트 우선, 텍스트 렌더링과 키보드 포커스 표시를 통일한다.
- 좁은 화면에서도 최소 320px부터 깨지지 않는 기본 레이아웃을 보장한다.

## 다음 재구성 순서

1. **홈:** 영업용 소개와 기획 시작 진입을 분리한다. 핵심 가치는 한 문장, 산출물은 3개 묶음(기획·검토·배포)으로 압축한다.
2. **조건 입력:** 지역·기간·테마·대상을 단계형 입력으로 바꾸고, 생성 전 입력 요약을 고정한다.
3. **결과 상세:** 상단은 상품명/핵심 조건/내보내기만 두고, 산출물은 `기획 → 일정 → 견적 → 홍보`로 재배치한다.
4. **산출물:** 카드뉴스·블로그·카카오는 보조 탭으로 묶고, 일정표와 견적서는 현장에서 실제 사용하는 문서 우선 화면으로 만든다.
5. **브랜드 일관성:** 아이콘 이모지 의존을 줄이고, 텍스트 대비·행간·버튼 위계를 통일한다.

## 보호 원칙

- 관광공사 API, AI 생성, 저장/공유, PDF 출력의 기존 동작을 디자인 작업 중 바꾸지 않는다.
- API 키 및 프록시 구성은 UI 리디자인 범위에서 수정하지 않는다.
- 원본 작업 폴더의 미커밋 변경은 건드리지 않고 이 분기에서만 변경한다.
